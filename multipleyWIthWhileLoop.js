let coutning = 2;
let count = 1;
while(count <= 10){
    let sum=coutning*count;
    console.log(coutning + `X` + count + `=` + sum);
    count++;
}
for(count=1; count <= 10; count++){
    let sum=coutning*count;
    console.log(coutning + `X` + count + `=` + sum);

}